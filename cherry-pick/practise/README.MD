## Cherry-Pick Practise Lab

**Q**: Cherry pick with no conflicts tutorial 

- Create a folder `cherry-pick-no-conflict` and `git init` it
- Create a file `fileMain.txt` and add 3 commits with the  text and  commit messages being `commit1-main`,`commit2-main` and `commit3-main` respectively
- Rename `master` to `main`
- Create a new branch `branchB` and switch to it
- Now at the end of the file `fileMain.txt` add 3 commits  with the text and commit messages being `commit1-branchB` ,`commit2-branchB` and `commit3-branchB` respectively
- Switch to main
- Now cherry pick the 2 commits `commit1-branchB` and `commit2-branchB`

**Solution**:

- Create a folder `cherry-pick-no-conflict` and `git init` it ➡️ Follow the instructions
- Create a file `fileMain.txt` and add 3 commits to it respectively with the commit messages  and text being being `commit1-main`,`commit2-main` and `commit3-main` respectively ➡️ Follow the instructions
- Rename `master` to `main` ➡️ Follow the instructions
- Create a new branch `branchB` and switch to it ➡️ Follow the instructions
- Now at the end of the file `fileMain.txt` add 3 commits  with the text and commit messages being `commit1-branchB` ,`commit2-branchB` and `commit3-branchB` respectively ➡️ Follow the instructions
- Switch to main ➡️ Follow the instructions
- Now cherry pick the 2 commits `commit1-branchB` and `commit2-branchB`➡️ `git log branchB --oneline` then 